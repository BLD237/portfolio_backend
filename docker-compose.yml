version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: portfolio-backend
    restart: always
    ports:
      - "8085:8085"
    volumes:
      - ./portfolio.db:/app/portfolio.db
      - ./uploads:/app/uploads
    env_file:
      - .env
